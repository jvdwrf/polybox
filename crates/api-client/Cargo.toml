[package]
name = "zestors-api-client"
version = "0.1.0"
edition = "2024"

[dependencies]
ploidy-util = "0.15.0"

[features]
trace-context = ["tracing", "ploidy-util/trace-context"]
tracing = ["ploidy-util/tracing"]
